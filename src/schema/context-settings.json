{
    "description": "context settings",
    "type": "object",
    "properties": {
        "blenMode": {
            "$ref": "./blend-mode.json"
        },
        "opacity": {
            "type": "number",
            "default": 1
        }
    },
    "required": [
        "blenMode",
        "opacity"
    ],
    "additionalProperties": false
}